# Firstock Integration

Firstock integration is deliberately deferred until Phase 3. Before coding it, we will verify the current official Firstock Developer API documentation and isolate its exact endpoints, authentication flow, WebSocket V2 feed contracts, subscriptions, heartbeats, and reconnect semantics behind a backend-only adapter. No credentials are sent to the browser.
